const apiUrl = 'http://localhost:63613';
const api = "api";

const urls = {
    signalRUrl: `${apiUrl}/`,
    getAllUsers: `${apiUrl}/users/all`,
    getAllRoles: `${apiUrl}/roles/all`,
    newsApiUrl: apiUrl,
};

export {
    urls as Urls,
};
